events {
    worker_connections 1024;
}

http {
    # Evennia internal webserver (from WEBSERVER_PORT_INTERNAL=5001)
    upstream evennia_http {
        server 127.0.0.1:5001;
    }

    # Evennia WebSocket server (default port 8001)
    upstream evennia_ws {
        server 127.0.0.1:8001;
    }

    server {
        listen ${PORT} default_server;
        server_name _;

        # HTTP requests proxy to Evennia Portal
        location / {
            proxy_pass http://evennia_http;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # WebSocket endpoint - clients connect to /ws
        # This proxies to the internal WebSocket server on port 8001
        location /ws {
            # WebSocket requires HTTP/1.1
            proxy_http_version 1.1;

            # Pass the WebSocket upgrade headers
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";

            # Standard proxy headers
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # WebSocket specific settings
            proxy_pass http://evennia_ws;
            proxy_read_timeout 86400s;
            proxy_send_timeout 86400s;
        }

        # Health check endpoint for Railway
        location /health {
            return 200 'OK';
            add_header Content-Type text/plain;
        }
    }
}